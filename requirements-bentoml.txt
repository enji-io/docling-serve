# BentoML requirements for docling-serve
# This file includes all runtime dependencies needed for BentoML deployment

# Core docling dependencies
docling~=2.38
docling-core>=2.45.0
docling-jobkit[kfp,rq,vlm]>=1.8.0,<2.0.0

# FastAPI and web framework
fastapi[standard]<0.129.0
httpx~=0.28
python-multipart>=0.0.14,<0.1.0
websockets~=14.0
scalar-fastapi>=1.0.3

# Data validation and settings
pydantic~=2.10
pydantic-settings~=2.4

# CLI dependencies
typer~=0.12
rich

# OpenTelemetry and observability
opentelemetry-api>=1.36.0,<2.0.0
opentelemetry-sdk>=1.36.0,<2.0.0
opentelemetry-exporter-otlp>=1.15.0
opentelemetry-instrumentation-fastapi>=0.57b0,<0.58
opentelemetry-exporter-prometheus>=0.57b0
prometheus-client>=0.21.0

# MCP support
docling-mcp>=1.0.0

# PyTorch (choose appropriate version based on deployment)
# For CPU-only deployments, use:
# torch>=2.7.1
# torchvision>=0.22.1
# For CUDA deployments, install from appropriate PyTorch index
torch>=2.7.1
torchvision>=0.22.1

# Optional UI dependencies (uncomment if needed)
# gradio>=5.23.2,<6.0.0

# Optional OCR dependencies (uncomment if needed)
# tesserocr~=2.7
# easyocr>=1.7
# rapidocr>=3.3,<4.0.0 ; python_version < '3.14'
# onnxruntime>=1.7.0,<2.0.0

# BentoML
bentoml>=1.2.0
